<!DOCTYPE html>
<html>
  <head>
    <title>Webapp</title>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
      .topnav {
            background-color: #333;
            overflow: auto;
        }
        
        /* Style the links inside the navigation bar */
        .topnav a {
            float: right;
            display: block;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }
        
        /* Change the color of links on hover */
        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }
        
        /* Add a color to the active/current link */
        .topnav a.active {
            background-color: #4CAF50;
            color: white;
        }

        #home {
            float: left;
            font-weight: bold;
            /*font-size: 25px;*/
        }

    </style>

  </head>

  <body>
      <nav class="topnav" id="myTopnav">
            <a href="/index.erb">Sell</a>
            <!-- <a href="#contact">Contact</a>
            <a href="#news">News</a> -->
            <a><%= link_to "Sign up", new_user_registration_path %></a>
            <% if user_signed_in? %>
              <a><%= link_to "Sign out", destroy_user_session_path, method: :delete %></a>
            <% else %>
              <a><%= link_to "Log in", new_user_session_path %></a>
            <% end %>
            <a id="home" href="/">Hungry</a>
            <!-- # <a id="home"><%= link_to "Hungry", root_path %></a> -->
      </nav>


      <!-- <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">
                <%= link_to "Hungry", root_path, class: "navbar-brand" %>
            </div>
            <ul class="nav navbar-nav">
                <li><%= link_to "Sign up", new_user_registration_path %></li>
                <% if user_signed_in? %>
                    <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
                <% else %>
                    <li><%= link_to "Log in", new_user_session_path %></li>
                <% end %>
            </ul>
        </div>
      </nav> -->
     <!--  <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p> -->

      <%= yield %>

  </body>
</html>
